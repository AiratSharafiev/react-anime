import { addContent, resetCount } from "./reducers/currentPage.slice";
import { setCurrentGenre } from './reducers/currentGenre.slice';
import { toggleClass, iconMenu, closeIconMenu } from './reducers/setMenu.slice';

export {
    addContent,
    resetCount,
    setCurrentGenre,
    toggleClass, 
    iconMenu, 
    closeIconMenu
};